name: Telegram New Command

on:
  schedule:
    # Poll Telegram for /new every 5 minutes.
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  poll-new-command:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Process /new commands
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_IDS: ${{ secrets.TELEGRAM_CHAT_IDS }}
          PROCESS_TELEGRAM_COMMANDS: "true"
          SEND_REPORT_WHEN_NO_COMMANDS: ${{ github.event_name == 'workflow_dispatch' }}
          TIMEZONE: Asia/Novosibirsk
          HTTP_TIMEOUT_SECONDS: "20"
        run: python src/bot.py
